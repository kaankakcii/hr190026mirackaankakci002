<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hr190026.mirac_kaan_kakci_final">

    <application
        android:name=".GezegenlerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HR190026_MiracKaanKakciFinal"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.GezegenDetail"
            android:theme="@style/AppThemeFullScreenBlack"

            ></activity>
        <activity
            android:name=".activity.ListActivity"
            android:theme="@style/AppThemeFullScreenBlack"></activity>
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/AppThemeFullScreenBlack">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>